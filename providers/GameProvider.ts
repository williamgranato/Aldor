// Adaptador simples de GameProvider.
// Se você souber o caminho real do seu useGame, substitua a linha abaixo pelo re-export correto.
export function useGame(): any {
  return null as any; // Retorna null para que o mercado use estado padrão
}
