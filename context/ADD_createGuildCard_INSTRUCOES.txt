// context/GameProvider_aldor_client.tsx — trechos a adicionar/atualizar

// 1) No tipo do contexto (Ctx), adicione:
/*
  createGuildCard: (info:{name:string, origin:string, role:string})=>void;
*/

// 2) Na implementação do provider, crie a função:
/*
  const createGuildCard = (info:{name:string, origin:string, role:string})=>{
    setState(s=>{
      const rank = 'F' as any;
      const guild = { ...s.guild, isMember:true, memberCard:{ name:info.name, origin:info.origin, role:info.role, rank } };
      const player = { ...s.player, adventurerRank: rank };
      return { ...s, guild, player, updatedAt: Date.now() };
    });
  };
*/

// 3) Exporte no value do contexto (ctx):
/*
  const ctx = {
    ...,
    createGuildCard,
  };
*/

// 4) Opcional: se sua tela desconta 1 prata ANTES de chamar createGuildCard, não precisa debitar aqui.
//    Caso queira centralizar a cobrança, mova o `takeCoins({silver:1})` para dentro da função e verifique saldo.
